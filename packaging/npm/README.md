# npm wrapper package (arrives with the first release)

Distribution channel per design D20: a thin run.js + per-platform binaries
(the esbuild/lark-cli pattern), or the cargo-dist npm installer. Primary
channels remain brew / cargo / curl.
