# ism agent skill (arrives in M3)

This directory is the `npx skills add`-convention location. The embed source
of truth will live at `crates/ism-cli/skill/` and be installed version-locked
via `ism skill install`; CI keeps both copies byte-identical.
